[project]
name = "fastqueue"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2",
    "django-environ>=0.12.0",
    "isort>=6.0.1",
    "ruff>=0.11.7",
    "sphinx>=8.1.3",
]
